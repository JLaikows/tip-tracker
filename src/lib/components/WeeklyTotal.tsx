import { FC } from "react";
import StyledCard from "./StyledCard";

const WeeklyTotal: FC = () => {
  return <StyledCard title="Total Weekly Amount">Temp Card</StyledCard>;
};

export default WeeklyTotal;
